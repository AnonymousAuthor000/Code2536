/* Copyright 2017 The TensorFlow Authors. All Rights Reserved.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
==============================================================================*/
#include "tensorflow/lite/kernels/internal/optimized/integer_ops/conv.h"

#include <stddef.h>
#include <iostream>
#include <cstdint>
#include <vector>

// Only use multi-threaded Eigen if ruy is disabled.
#if !defined(TFLITE_WITH_RUY)
#define TFLITE_WITH_MULTITHREADED_EIGEN
#endif

#include "tensorflow/lite/c/builtin_op_data.h"
#include "tensorflow/lite/c/common.h"
#include "tensorflow/lite/kernels/cpu_backend_context.h"
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
#include "tensorflow/lite/kernels/eigen_support.h"
#endif
#include "tensorflow/lite/kernels/internal/compatibility.h"
#include "tensorflow/lite/kernels/internal/types.h"
// b/131835803 forces us to include multithreaded_conv.h before optimized_ops.h
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
#include "tensorflow/lite/kernels/internal/optimized/multithreaded_conv.h"
#endif
#include "tensorflow/lite/kernels/internal/optimized/optimized_ops.h"
#include "tensorflow/lite/kernels/internal/quantization_util.h"
#include "tensorflow/lite/kernels/internal/reference/conv.h"
#include "tensorflow/lite/kernels/internal/reference/integer_ops/conv.h"
#include "tensorflow/lite/kernels/internal/tensor.h"
#include "tensorflow/lite/kernels/internal/tensor_ctypes.h"
#include "tensorflow/lite/kernels/internal/tensor_utils.h"
#include "tensorflow/lite/kernels/kernel_util.h"
#include "tensorflow/lite/kernels/padding.h"
#include "tensorflow/lite/util.h"

namespace tflite {
namespace ops {
namespace custom {
namespace krubxg {

// This file has 4 implementation of Conv.
enum KernelType {
  kReference,
  kGenericOptimized,  // Neon-free
  // kMultithreadOptimized is a mixture of an Eigen-based kernel when threads
  // are available and kGenericOptimized when we must use only one thread.
  kMultithreadOptimized,
  // The kernel uses use CBLAS interface for matrix multiplication.
  // It's fast when an optimized CBLAS implementation is available (e.g. Apple
  // Accelerate Framework), and it's slow when falling back to naive
  // implementation.
  kCblasOptimized,
};

const int kTensorNotAllocated = -1;

static constexpr size_t kMaxIm2colBufferSizeMobile = 1024 * 1024 * 1024;  // 1GB

const float filter_raw[2048]={0.3051340878009796, 0.01583755947649479, -0.09126973152160645, 0.004308145493268967, 0.04909156262874603, 0.2570631206035614, 0.042566314339637756, 0.14823777973651886, 0.038970138877630234, -0.27149051427841187, 0.05950004979968071, -0.08193938434123993, 0.16028600931167603, 0.19559729099273682, -0.08568011969327927, -0.06330470740795135, 0.022170504555106163, -0.12988963723182678, -0.02037045918405056, 0.2642027735710144, -0.1238105520606041, -0.31594303250312805, 0.20811691880226135, 0.006037214770913124, -0.09945943206548691, 0.0931856706738472, 0.1639169454574585, -0.08651632070541382, 0.3073662519454956, 0.12290188670158386, 0.15860024094581604, 0.07757137715816498, -0.09695108234882355, 0.28719234466552734, -0.01599699817597866, 0.1773413121700287, 0.21239455044269562, 0.10671736299991608, 0.14271759986877441, -0.01200359221547842, 0.030103344470262527, 0.0378897488117218, 0.35484230518341064, 0.43523362278938293, -0.04598403349518776, -0.19697535037994385, 0.18709836900234222, 0.09571782499551773, 0.004605681635439396, 0.061669811606407166, -0.009614457376301289, -0.1438423991203308, 0.08659229427576065, 0.16058579087257385, 0.21934205293655396, 0.12776297330856323, -0.33702942728996277, -0.05065888911485672, -0.06307468563318253, 0.015304791741073132, 0.0561746209859848, 0.021791672334074974, 0.1320337951183319, -0.05583931878209114, -0.06741417199373245, -0.01833539828658104, 0.039055027067661285, 0.08164302259683609, -0.004544390831142664, 0.048885274678468704, 0.042805738747119904, 0.07348497956991196, 0.07145673781633377, -0.23154282569885254, 0.09198049455881119, -0.06467562168836594, 0.04879004880785942, -0.37719205021858215, -0.10809729993343353, 0.02429487369954586, 0.0484219454228878, -0.04392730072140694, 0.1823786199092865, -0.15828575193881989, -0.09737033396959305, 0.028482457622885704, 0.015872878953814507, -0.1573971062898636, 0.07943816483020782, 0.24425837397575378, 0.1433977633714676, 0.03918306902050972, 0.014249064959585667, -0.16732338070869446, 0.05095762759447098, 0.437583327293396, -0.1864473819732666, 0.008363856002688408, 0.15115712583065033, -0.08411598950624466, -0.014341522939503193, -0.15666526556015015, 0.2212587296962738, -0.03421839326620102, -0.009794254787266254, -0.13446293771266937, 0.06353238224983215, -0.06705999374389648, -0.11663331091403961, -0.19070720672607422, -0.005392193328589201, 0.01416044682264328, 0.07018163800239563, -0.30108317732810974, -0.08616558462381363, -0.2757716774940491, 0.11191410571336746, -0.036190666258335114, -0.00039813530747778714, 0.19713632762432098, 0.0126984603703022, 0.03365548327565193, 0.25645267963409424, 0.27453741431236267, 0.10988260805606842, 0.12604667246341705, 0.023682717233896255, -0.06601107120513916, 0.012716120108962059, 0.07263482362031937, -0.3038148880004883, 0.04584397003054619, 0.11747187376022339, 0.004494982771575451, 0.14349563419818878, 0.0712599977850914, -0.01697648875415325, -0.18293653428554535, -0.11084707826375961, -0.13476605713367462, 0.060741692781448364, 0.034486863762140274, 0.0332004651427269, 0.45180273056030273, -0.02828100137412548, -0.3853009343147278, -0.12588411569595337, 0.18736746907234192, -0.15822137892246246, 0.02168099209666252, -0.3052579462528229, -0.15344956517219543, -0.0606820285320282, -0.10554343461990356, 0.008281095884740353, -0.1191953495144844, -0.04279094561934471, -0.3898511528968811, -0.03946917876601219, -0.1884908229112625, 0.1654103547334671, -0.1256885975599289, 0.11478347331285477, 0.281690388917923, -0.1634741723537445, 0.06496356427669525, -0.11715418100357056, -0.21559175848960876, 0.12740856409072876, -0.144300177693367, 0.16309931874275208, -0.09661592543125153, 0.054129134863615036, -0.22406241297721863, -0.16353628039360046, 0.0902397409081459, 0.05261233076453209, -0.06703927367925644, -0.06071113422513008, 0.05694273114204407, -0.06521493196487427, 0.13735616207122803, -0.008646736852824688, 0.04054552689194679, 0.31907129287719727, -0.1307733803987503, 0.20642352104187012, -0.14295080304145813, -0.10385043174028397, 0.11430058628320694, -0.18415771424770355, 0.023550650104880333, 0.03804050013422966, 0.03800885006785393, -0.005332177970558405, -0.034736864268779755, 0.03618333488702774, 0.04005158692598343, 0.02889513410627842, 0.08245966583490372, 0.36195340752601624, -0.030155088752508163, 0.051567401736974716, -0.13895559310913086, -0.1575148105621338, -0.1606764942407608, 0.5494463443756104, -0.039462022483348846, -0.0400906577706337, -0.20341819524765015, 0.07969950139522552, -0.07870400696992874, 0.4853455722332001, -0.019361410290002823, -0.06895878165960312, -0.14365671575069427, -0.01212393119931221, 0.062274299561977386, -0.11740581691265106, -0.21558542549610138, 0.05394153296947479, -0.01460357941687107, -0.014872504398226738, 0.009293102659285069, -0.004439995624125004, -0.07304555922746658, -0.06924890726804733, 0.052807845175266266, 0.28404882550239563, -0.12699148058891296, -0.09980687499046326, 0.11645019799470901, -0.050505708903074265, -0.02778591401875019, 0.057010576128959656, -0.01587206870317459, 0.10651331394910812, 0.004086622502654791, -0.06780125200748444, 0.048700228333473206, -0.1922736018896103, -0.05173192173242569, 0.039180364459753036, -0.07860325276851654, -0.006843413691967726, 0.28998374938964844, -0.03436928614974022, -0.21341773867607117, 0.12750668823719025, 0.6846702694892883, -0.018238335847854614, 0.033815596252679825, 0.4712389409542084, -0.16966989636421204, -0.2932486832141876, 0.011923643760383129, 0.1356642246246338, 0.060094382613897324, 0.2560606002807617, -0.04968167468905449, -0.10418140888214111, 0.02842128835618496, -0.009118740446865559, 0.04315982386469841, -0.017341528087854385, 0.005206875968724489, -0.016748353838920593, -0.10657837986946106, 0.03299926221370697, 0.1875268965959549, -0.09313667565584183, -0.07250258326530457, -0.0009915797272697091, -0.024860193952918053, 0.10520974546670914, 0.15099181234836578, 0.08803118020296097, 0.05553760007023811, 0.05175945535302162, -0.13337688148021698, 0.05675136297941208, 0.0204465314745903, -0.031200626865029335, -0.01894202083349228, -0.06798211485147476, 0.018621964380145073, -0.11724784225225449, 0.13100673258304596, -0.07391679286956787, -0.1450926512479782, 0.0872877910733223, 0.19969215989112854, -0.15763375163078308, -0.4865794777870178, 0.17282569408416748, 0.6979405283927917, 0.48502928018569946, -0.23651869595050812, -0.11966033279895782, -0.25895756483078003, -0.5783679485321045, -0.15311278402805328, -0.409605473279953, 0.30487969517707825, -0.07826121151447296, -0.057648513466119766, -0.10115005075931549, 0.3636784553527832, -0.08933061361312866, -0.06630212813615799, 0.005754930432885885, -0.07340768724679947, -0.009147933684289455, 0.046505894511938095, 0.008296655490994453, 0.06346454471349716, 0.08006183803081512, 0.2272944301366806, 0.04262449964880943, 0.10182023793458939, 0.06662504374980927, 0.005933172535151243, -0.10645292699337006, -0.019779030233621597, -0.02731930837035179, 0.22076261043548584, -0.11007305234670639, 0.056054964661598206, 0.46404939889907837, -0.11160983890295029, 0.19827976822853088, -0.13697287440299988, -0.03656837344169617, 0.17057059705257416, -0.20758400857448578, -0.048904478549957275, 0.04189271479845047, 0.07950733602046967, 0.02580566331744194, -0.11941734701395035, -0.3308035731315613, -0.023676695302128792, 0.002020511543378234, -0.10342621803283691, -0.010790913365781307, 0.06476534157991409, -0.041496261954307556, 0.06401419639587402, 0.019133294001221657, 0.06167954206466675, 0.017091698944568634, 0.07790049910545349, -0.08811764419078827, -0.16721180081367493, -0.20316950976848602, 0.08977307379245758, 0.11458463221788406, -0.041369445621967316, -0.0001464269880671054, -0.046754397451877594, 0.05501663684844971, -0.22872720658779144, 0.08380462229251862, 0.048801835626363754, 0.020874932408332825, -0.04396511986851692, 0.1565018743276596, -0.1271035522222519, 0.04300914332270622, -0.07383532077074051, 0.14000198245048523, 0.12477705627679825, -0.04756080359220505, -0.16126880049705505, -0.2898903787136078, -0.10191100090742111, -0.17584587633609772, 0.0008739391341805458, 0.32323160767555237, -0.08097067475318909, 0.12125515192747116, 0.022436801344156265, -0.07432888448238373, 0.036217473447322845, 0.10528521239757538, -0.04788976162672043, 0.0821443721652031, 0.049868933856487274, -0.1438472419977188, -0.11437442898750305, -0.1012282595038414, 0.30841419100761414, -0.41826608777046204, -0.08303224295377731, 0.15277746319770813, 0.02890666574239731, -0.26660943031311035, 0.10799670219421387, -0.23206162452697754, 0.15047618746757507, 0.09714045375585556, -0.03642887994647026, -0.08487632125616074, 0.17046353220939636, -0.06539938598871231, 0.626072347164154, 0.0936412513256073, 0.030516617000102997, 0.25373080372810364, -0.2009810507297516, 0.1158931627869606, -0.08307463675737381, 0.27014410495758057, 0.13129469752311707, 0.03961341083049774, 0.017034253105521202, 0.199783593416214, 0.2773338854312897, 0.024566613137722015, -0.12698806822299957, -0.15287165343761444, -0.045196473598480225, -0.014430610463023186, 0.1829308420419693, -0.01630658656358719, 0.01322926115244627, 0.1477641612291336, 0.2633070647716522, 0.06440999358892441, 0.1973462998867035, 0.021846653893589973, -0.1391981840133667, -0.29848453402519226, -0.0013014331925660372, 0.017039721831679344, -0.04036278650164604, -0.027443014085292816, -0.18282142281532288, 0.0075340657494962215, -0.005443422589451075, -0.21765223145484924, 0.42749449610710144, 0.006068707909435034, 0.08301886171102524, -0.09166211634874344, 0.09393689781427383, 0.07960407435894012, 0.09452663362026215, -0.24300776422023773, -0.0505480021238327, -0.1374899297952652, 0.05898863077163696, -0.12076149135828018, -0.2317861020565033, 0.14690977334976196, 0.2496587634086609, -0.5829501748085022, 0.36827465891838074, -0.01603475958108902, -0.19485510885715485, -0.17720134556293488, -0.07643686234951019, 0.38276955485343933, 0.12086314707994461, -0.09089776128530502, 0.09798065572977066, -0.18915018439292908, -0.08875946700572968, 0.01161832083016634, 0.6861172318458557, 0.004664548672735691, 0.2383142113685608, 0.21728135645389557, 0.03166462481021881, 0.1830260008573532, -0.201175257563591, 0.09909786283969879, -0.041925057768821716, 0.06864438205957413, -0.04295564442873001, -0.09427543729543686, 0.008134723640978336, 0.04874679818749428, -0.06694378703832626, 0.0281230341643095, 0.04555973783135414, 0.048762381076812744, -0.09466247260570526, -0.3933250904083252, 0.17616599798202515, -0.10650090873241425, -0.012019029818475246, -0.11511915177106857, -0.01663510873913765, 0.33423125743865967, -0.11381193995475769, 0.54277104139328, 0.04549705609679222, -0.14298716187477112, 0.16441741585731506, 0.10131436586380005, 0.18832004070281982, -0.37357664108276367, 0.014884957112371922, 0.11646091192960739, 0.40326017141342163, 0.09202408045530319, 0.08449966460466385, 0.03946070745587349, -0.06217704340815544, 0.03568313643336296, -0.13231556117534637, -0.1071125715970993, -0.0088746203109622, 0.0600191093981266, 0.011425788514316082, 0.014139881357550621, -0.4069516658782959, -0.21126314997673035, -0.13805079460144043, -0.018040161579847336, -0.0015574004501104355, 0.3827389180660248, 0.018865486606955528, 0.34918197989463806, 0.09605519473552704, -0.13802552223205566, 0.12960860133171082, -0.14537973701953888, -0.012388880364596844, -0.046026561409235, 0.04887707531452179, -0.0023867697454988956, -0.26215553283691406, 0.04706627130508423, -0.012011880055069923, 0.24452784657478333, -0.09729032963514328, 0.015697002410888672, 0.039893988519907, -0.1741645634174347, 0.12663395702838898, 0.052256520837545395, -0.027188405394554138, -0.15412412583827972, 0.1120835542678833, -0.1913558393716812, -0.06924117356538773, 0.07077859342098236, -0.020859409123659134, -0.030891431495547295, 0.1390221267938614, 0.05998609587550163, 0.028445545583963394, -0.0034770218189805746, 0.0845869779586792, -0.09847991913557053, -0.2990986406803131, -0.0445123054087162, 0.004354575648903847, 0.1393015831708908, -0.0390004999935627, -0.06581642478704453, 0.10098988562822342, -0.02944716438651085, 0.05788528174161911, 0.065535768866539, 0.002836186671629548, 0.06099174916744232, 0.22478152811527252, -0.3277188837528229, 0.08242414891719818, 0.09253999590873718, 0.0977935791015625, 0.02877087891101837, -0.04281512275338173, -0.09022500365972519, -0.05794629454612732, 0.0902949869632721, 0.01386466808617115, -0.008004908449947834, -0.012945865280926228, -0.10656698048114777, 0.07900464534759521, 0.28877368569374084, 0.2826516628265381, 0.07133867591619492, 0.0026677760761231184, 0.08181989938020706, 0.2736964821815491, -0.05113052576780319, 0.11241378635168076, 0.07439012080430984, 0.6123997569084167, 0.18533246219158173, 0.16336867213249207, 0.0183311328291893, 0.030405418947339058, -0.00902341678738594, -0.16141727566719055, 0.015200747177004814, 0.08919022977352142, -0.19864234328269958, -0.12111380696296692, 0.043529998511075974, 0.03889380022883415, -0.03131623938679695, 0.12881635129451752, 0.23283319175243378, 0.09326907992362976, -0.15436246991157532, 0.06580507010221481, 0.057342324405908585, 0.10133261978626251, -0.16207581758499146, 0.07439538836479187, -0.05260109528899193, -0.024195797741413116, 0.08593752235174179, -0.046097222715616226, -0.2418704777956009, 0.1541847437620163, -0.048257336020469666, -0.19091595709323883, 0.06713394075632095, -0.026189275085926056, -0.01856354810297489, -0.016977734863758087, -0.026611801236867905, 0.20459701120853424, -0.0456254668533802, -0.045655377209186554, -0.32306355237960815, -0.11125568300485611, -0.01686163805425167, 0.011070297099649906, 0.013401931151747704, 0.08277179300785065, -0.48942744731903076, -0.03842445835471153, 0.02583167515695095, 0.0002866623399313539, 0.10092226415872574, 0.038272686302661896, 0.02625303342938423, -0.2092786282300949, 0.1171664297580719, 0.019431624561548233, -0.017566589638590813, 0.17573392391204834, 0.19869044423103333, -0.0257082749158144, -0.08525724709033966, 0.43272101879119873, -0.35189637541770935, 0.026827087625861168, -0.1544172465801239, 0.07359138876199722, -0.0678492859005928, 0.3243749439716339, 0.113609679043293, 0.0394730418920517, -0.03961852937936783, 0.4511488974094391, -0.0897807702422142, -0.18721897900104523, -0.22878436744213104, 0.08110611885786057, -0.03596221283078194, 0.16674275696277618, -0.0027536980342119932, -0.05788682401180267, -0.06549075990915298, -0.005688963923603296, 0.1589231938123703, -0.14123938977718353, -0.0737023577094078, 0.028136759996414185, -0.14005395770072937, -0.038088422268629074, -0.05273452773690224, -0.0029033347964286804, -0.004238442983478308, -0.17283344268798828, -0.16824354231357574, 0.044149622321128845, -0.03258849307894707, 0.016093747690320015, 0.056056682020425797, 0.019539548084139824, -0.15113916993141174, 0.043617743998765945, -0.01991170085966587, 0.11574596166610718, -0.022789988666772842, 0.04728042706847191, -0.13866491615772247, -0.13023120164871216, 0.07481175661087036, -0.03929136320948601, 0.5063424110412598, -0.015357567928731441, 0.02503247559070587, 0.04099036753177643, 0.03380898758769035, -0.2998637557029724, -0.024961624294519424, -0.07075135409832001, -0.06478504091501236, 0.016124943271279335, -0.1098674088716507, -0.033351004123687744, 0.05016230046749115, 0.0340862013399601, -0.0918445885181427, 0.04914337396621704, -0.028630955144762993, 0.10310598462820053, 0.038151971995830536, -0.030224623158574104, 0.08967199176549911, 0.0707140639424324, -0.01761140115559101, -0.08998803049325943, 0.06092536821961403, 0.3001639246940613, 0.009599666111171246, 0.07565910369157791, 0.2131686806678772, -0.26075395941734314, -0.05457664653658867, 0.026023875921964645, -0.08962280303239822, 0.06351341307163239, -0.14626441895961761, -0.0048943390138447285, -0.1002613753080368, 0.05483350157737732, 0.1282285898923874, -0.02685973048210144, 0.02078668400645256, 0.03364831209182739, -0.01232469454407692, -0.16897590458393097, -0.03468458354473114, -0.32136020064353943, 0.2347770631313324, 0.12745371460914612, 0.09809412062168121, 0.09783606976270676, -0.06405195593833923, 0.005275797564536333, -0.04121672734618187, -0.33792340755462646, 0.01804990880191326, -0.08427226543426514, -0.1127583310008049, 0.019452612847089767, 0.07391433417797089, 0.11978422850370407, -0.0434265211224556, 0.026824330911040306, 0.18069761991500854, 0.13190528750419617, -0.037563759833574295, -0.028242776170372963, -0.0024458589032292366, -0.1033090278506279, 0.08214573562145233, -0.08816805481910706, -0.0055055818520486355, 0.20572687685489655, -0.001740429550409317, -0.404806911945343, -0.042763594537973404, -0.13913317024707794, -0.027564143761992455, 0.01836526207625866, -0.18570803105831146, 0.21795840561389923, -0.03618670254945755, 0.03029545396566391, 0.12282615900039673, 0.03372254967689514, -0.21489281952381134, 0.04898485913872719, -0.015217792242765427, 0.008636421523988247, 0.07966013252735138, 0.033072009682655334, -0.09389587491750717, -0.06424758583307266, 0.33545607328414917, 0.031068695709109306, -0.08898515999317169, 0.23663963377475739, -0.4585292637348175, 0.04849142208695412, -0.4024125039577484, 0.07930883020162582, 0.056889668107032776, 0.25101470947265625, -0.08526208996772766, 0.3246065378189087, -0.31724125146865845, -0.016868337988853455, -0.01674932800233364, -0.16651001572608948, -0.19685162603855133, 0.03789700195193291, 0.08514968305826187, -0.0022299946285784245, -0.1567874401807785, -0.00939150433987379, -0.1377234160900116, 0.018887316808104515, 0.11874500662088394, 0.0840640664100647, 0.058148957788944244, 0.31580379605293274, 0.5021078586578369, -0.0023499957751482725, 0.07637406140565872, 0.08158176392316818, -0.006001438479870558, -0.00786370225250721, 0.17081384360790253, -0.02253369428217411, -0.03968427702784538, -0.016342120245099068, -0.015988938510417938, 0.0096855154260993, 0.1826675683259964, 0.06296900659799576, 0.07138713449239731, 0.0931660383939743, 0.0473070926964283, 0.15285781025886536, 0.017392857000231743, -0.023102151229977608, -0.6958773136138916, -0.01621631532907486, 0.02350718155503273, 0.14424872398376465, 0.16693197190761566, -0.06650637835264206, 0.38285163044929504, 0.05767973139882088, 0.11118095368146896, 0.008807572536170483, 0.4844987690448761, 0.11763281375169754, -0.013658537529408932, 0.05200880393385887, -0.10003979504108429, 0.16480889916419983, 0.05438610166311264, 0.2395283579826355, 0.11777802556753159, 0.22399330139160156, -0.011866124346852303, -0.07539920508861542, 0.07713682949542999, -0.21141672134399414, -0.016078194603323936, -0.04826999828219414, -0.0006977812154218554, -0.15652453899383545, -0.4395304024219513, 0.02757233753800392, 0.16146011650562286, -0.08511894196271896, 0.024481715634465218, 0.020621493458747864, 0.036639317870140076, 0.1246667429804802, 0.15375222265720367, -0.06766964495182037, 0.005439722910523415, 0.021217459812760353, -0.014394854195415974, 0.21353627741336823, -0.0970073714852333, 0.007693501655012369, 0.1972624957561493, 0.03389769420027733, 0.03561703860759735, 0.13518595695495605, -0.06404320150613785, 0.023765474557876587, 0.06548751145601273, 0.1275469809770584, -0.12074685841798782, 0.13715825974941254, 0.12664519250392914, -0.03152260556817055, -0.17051507532596588, -0.08280083537101746, -0.10573404282331467, 0.06332271546125412, -0.0901857316493988, -0.3519430458545685, -0.21602533757686615, -0.19297674298286438, -0.024025211110711098, -0.07060865312814713, -0.04720430448651314, 0.010935340076684952, -0.3535281717777252, 0.06177453324198723, -0.1827070415019989, -0.06259117275476456, -0.14462310075759888, -0.06711366772651672, -0.1409953534603119, 0.26144710183143616, 0.12926869094371796, -0.08655419200658798, -0.027686886489391327, 0.05969064310193062, -0.07465575635433197, -0.21541714668273926, -0.19366687536239624, 0.05887038633227348, -0.16850590705871582, 0.07672562450170517, 0.08883287012577057, -0.10704001039266586, 0.15629953145980835, -0.02113383077085018, 0.07108558714389801, 0.08338761329650879, 0.029269540682435036, 0.15648846328258514, 0.02634991519153118, -0.1102801114320755, -0.16750822961330414, -0.04983840882778168, -0.061280444264411926, 0.42957189679145813, -0.32532453536987305, 0.027274109423160553, -0.15419968962669373, 0.08083110302686691, 0.05535844340920448, -0.014819350093603134, 0.06709182262420654, 0.061672501266002655, 0.023463444784283638, 0.05635283887386322, -0.16059696674346924, 0.026997582986950874, 0.08113272488117218, 0.026317842304706573, 0.041291072964668274, 0.12701909244060516, -0.02163107693195343, 0.025604551658034325, 0.16558192670345306, 0.2046707421541214, -0.09698626399040222, 0.07402461022138596, -0.07942505925893784, -0.042439304292201996, 0.025525733828544617, -0.09762023389339447, -0.03434523195028305, 0.11283775418996811, 0.21077357232570648, 0.014864198863506317, 0.10069721192121506, -0.21497461199760437, 0.2694022059440613, -0.05547883361577988, 0.1168939545750618, -0.12556391954421997, -0.06016058102250099, -0.0012802665587514639, 0.0767069160938263, -0.010349824093282223, -0.0792577937245369, -0.06074066832661629, 0.11097200214862823, 0.05852547660470009, -0.12429600208997726, -0.03530886396765709, -0.17620570957660675, -0.1125742718577385, -0.08871843665838242, 0.30430153012275696, -0.14628788828849792, 0.22912205755710602, 0.06672842800617218, 0.052747827023267746, -0.04148432984948158, -0.05221820995211601, 0.05679316818714142, 0.27792561054229736, 0.08291591703891754, 0.0555415004491806, -0.3318179249763489, 0.04568278789520264, 0.11079941689968109, 0.04253913089632988, -0.055891003459692, 0.1057913601398468, -0.3281131386756897, -0.0040774098597466946, -0.047136206179857254, -0.1997147798538208, 0.02098359540104866, -0.05357563868165016, -0.28246384859085083, 0.03499392792582512, 0.03508944436907768, 0.09547524154186249, 0.03909774869680405, -0.006733518559485674, -0.014819171279668808, 0.030657300725579262, 0.028021175414323807, -0.11383088678121567, 0.004230399150401354, 0.009608336724340916, 0.061548229306936264, 0.18801087141036987, -0.012872654013335705, -0.05801777541637421, -0.09477883577346802, -0.032318275421857834, -0.006933491677045822, -0.006404523272067308, -0.016217133030295372, -0.028245823457837105, 0.05014835670590401, 0.03987628594040871, 0.01998257264494896, -0.049724459648132324, 0.0015370786422863603, -0.007216059137135744, 0.021850813180208206, -0.1247536689043045, 0.0005687261000275612, 0.07082744687795639, 0.021173492074012756, 0.029150770977139473, 0.02261585369706154, 0.09410152584314346, 0.05593691021203995, -0.05540867894887924, -0.004360475577414036, 0.06829816102981567, -0.009233792312443256, -0.03193337097764015, 0.05051384121179581, 0.10046760737895966, 0.04786079004406929, -0.086135633289814, 0.0024088597856462, -0.186265766620636, -0.007367593701928854, -0.08378421515226364, 0.01694001816213131, 0.1915113925933838, 0.14628036320209503, -0.1592061072587967, -0.0953802540898323, -0.06799515336751938, -0.04141472280025482, 0.05562584474682808, 0.11506239324808121, 0.043706927448511124, -0.055185869336128235, 0.011399966664612293, 0.15509682893753052, -0.01846390776336193, -0.024005088955163956, 0.0054823593236505985, -0.13912492990493774, -0.09197239577770233, -0.16651073098182678, -0.13972680270671844, 0.2055715173482895, -0.12377331405878067, -0.10814382135868073, -0.02112383395433426, 0.16281038522720337, 0.037416595965623856, 0.1966947317123413, 0.024880994111299515, -0.08321502059698105, -0.1269323229789734, 0.03926881402730942, -0.020942185074090958, 0.016975929960608482, 0.03485039249062538, 0.015012338757514954, 0.043506186455488205, -0.019924527034163475, -0.016218213364481926, -0.02712678350508213, -0.030642591416835785, -0.01995166949927807, 0.05450427904725075, -0.24109165370464325, -0.024271059781312943, -0.01850687712430954, 0.08200450986623764, 0.01333713624626398, -0.019585685804486275, 0.050405461341142654, -0.0319802351295948, -0.0110882967710495, -0.030150633305311203, -0.021864188835024834, 0.024678878486156464, 0.10062813758850098, -0.02592427097260952, -0.019244709983468056, -0.11272647231817245, 0.12358585000038147, 0.10709000378847122, 0.14946086704730988, 0.10792582482099533, 0.07632328569889069, 0.10209957510232925, 0.13419592380523682, 0.16478337347507477, 0.06477747112512589, 0.19028037786483765, -0.05671923607587814, 0.002003534696996212, 0.20792245864868164, 0.08122660964727402, 0.008690553717315197, -0.2733023464679718, 0.06650467216968536, -0.03238570690155029, 0.03805004060268402, 0.01882179081439972, -0.025793619453907013, 0.08165805041790009, 0.11243955045938492, -0.03993164002895355, 0.09519781172275543, -0.037576913833618164, 0.05069367587566376, -0.10051297396421432, 0.0034352666698396206, -0.09926553070545197, -0.06243322044610977, 0.013488896191120148, 0.0905957743525505, 0.0024510552175343037, -0.00968867540359497, 0.13815587759017944, -0.021462151780724525, 0.06918245553970337, 0.19427849352359772, -0.04615788534283638, 0.01816296949982643, 0.0845244973897934, 0.11680391430854797, -0.03148530423641205, -0.07699109613895416, -0.038073763251304626, -0.03486974164843559, -0.029103245586156845, 0.1921071708202362, 0.017595291137695312, -0.006830509752035141, -0.020425526425242424, 0.007438258267939091, -0.0975024402141571, 0.04386546462774277, -0.09754465520381927, -0.12658247351646423, -0.034051891416311264, -0.052432831376791, 0.015850063413381577, -0.005557234864681959, -0.11238371580839157, 0.10569151490926743, 0.044926129281520844, 0.049686312675476074, -0.0028489797841757536, 0.034331921488046646, 0.03924771025776863, 0.015887033194303513, 0.030367596074938774, 0.019994419068098068, -0.11075756698846817, 0.22159652411937714, -0.07008185237646103, 0.051235977560281754, 0.05780814588069916, -0.0657750815153122, 0.016073325648903847, -0.11686091870069504, 0.06616487354040146, -0.07360360026359558, -0.013491631485521793, 0.04738099128007889, 0.14881235361099243, -0.036154087632894516, 0.005496339872479439, 0.02524583600461483, 0.13245141506195068, -0.03309640288352966, -0.10197092592716217, -0.011956986039876938, 0.04015723615884781, -0.0460219569504261, -0.0011349532287567854, 0.03516046702861786, -0.01839006505906582, 0.06423277407884598, 0.053889330476522446, -0.2037658542394638, 0.03377841040492058, 0.040473874658346176, 0.02541366219520569, 0.04036394879221916, 0.09826778620481491, -0.5955500602722168, -0.009224854409694672, -0.004696850664913654, 0.01596473716199398, 0.2760360836982727, 0.08841017633676529, 0.20152276754379272, -0.002704378217458725, -0.10428230464458466, -0.014471049420535564, -0.04384634271264076, 0.08228996396064758, -0.027946794405579567, 0.11918561160564423, 0.06365443021059036, -0.044433705508708954, -0.029365506023168564, 0.09148167073726654, -0.0010487918043509126, 0.058999307453632355, -0.12155091762542725, -0.08478965610265732, 0.1397981494665146, 0.030439898371696472, -0.02720591239631176, 0.009665834717452526, 0.12184454500675201, -0.03809529170393944, 0.015308567322790623, -0.015600285492837429, 0.02072332799434662, 0.11249785125255585, 0.02447756752371788, -0.04616079106926918, -0.07093588262796402, -0.024504605680704117, -0.0031884065829217434, -0.032826703041791916, -0.0066465456038713455, -0.2082395851612091, 0.1247042566537857, -0.0679132342338562, 0.2334335744380951, 0.06158191338181496, -0.05483284965157509, 0.07578428834676743, -0.023032229393720627, 0.2993501126766205, 0.08830045908689499, -0.22448042035102844, 0.19411322474479675, 0.04883529618382454, 0.2227831929922104, 0.15912193059921265, -0.09601818025112152, 0.34496769309043884, -0.017180170863866806, -0.038121383637189865, 0.006318116094917059, -0.062078431248664856, 0.014491286128759384, 0.031575754284858704, -0.09082473069429398, -0.06084848940372467, -0.002079616067931056, -0.14643827080726624, 0.07953751087188721, -0.15224650502204895, 0.10394331812858582, -0.09016747027635574, 0.03792063146829605, -0.028965642675757408, 0.10260976105928421, 0.015581528656184673, -0.025200508534908295, -0.045323580503463745, 0.06492148339748383, 0.07644740492105484, 0.019546639174222946, 0.019854765385389328, 0.31341907382011414, -0.1367008239030838, -0.036463771015405655, 0.03821558877825737, 0.07122385501861572, 0.0005329816485755146, -0.03384467959403992, 0.0017080813413485885, 0.010590397752821445, -0.010385875590145588, 0.08934237062931061, 0.011435968801379204, -0.01746811345219612, 0.054883819073438644, 0.01945926435291767, -0.14335642755031586, -0.04164842516183853, 0.004458984825760126, 0.07587960362434387, 0.049947988241910934, -0.12695863842964172, -0.00818841252475977, 0.14378492534160614, 0.0018107465002685785, -0.052088405936956406, 0.009634129703044891, 0.006286470219492912, 0.050429169088602066, 0.006764150224626064, 0.02025504596531391, 0.1416061371564865, 0.05565490573644638, 0.0002909129543695599, 0.009101799689233303, -0.028466347604990005, 0.03435179218649864, -0.0007058640476316214, -0.007998947985470295, -0.05470868945121765, -0.16073083877563477, 0.06552620977163315, -0.019997429102659225, -0.05154634267091751, -0.021690577268600464, -0.032873786985874176, -0.09576910734176636, -0.011335058137774467, 0.04663778096437454, 0.05362991988658905, -0.030990585684776306, -0.03366440534591675, -0.04610664024949074, 0.0028342553414404392, -0.032403089106082916, -0.14352409541606903, -0.0074246847070753574, 0.0565519817173481, 0.003638750873506069, -0.0786810964345932, 0.05314157158136368, -0.0082276975736022, -0.04550304636359215, 0.09845098853111267, 0.040746744722127914, 0.09809083491563797, -0.012681486085057259, 0.010417893528938293, -0.0003930221137125045, -0.03140919283032417, 0.006211241707205772, -0.11221572011709213, -0.008965490385890007, -0.04524501785635948, -0.062209464609622955, 0.14285288751125336, -0.12806740403175354, -0.0340699777007103, 0.015524317510426044, 0.0385238379240036, 0.26937758922576904, -0.06396573036909103, 0.05779305100440979, -0.06572482734918594, -0.0529961921274662, -0.0836842805147171, -0.056273337453603745, 0.023751219734549522, 0.2828900218009949, 0.14754317700862885, -0.012737271375954151, 0.007223630789667368, -0.03668894246220589, -0.013161731883883476, 0.21883819997310638, 0.015992308035492897, 0.05032229423522949, 0.1518208086490631, -0.042345188558101654, -0.0394778735935688, -0.08015649020671844, 0.07092951983213425, -0.104625403881073, -0.037647396326065063, -0.0413317047059536, 0.07307254523038864, -0.00045566254993900657, -0.02563660778105259, -0.066779725253582, 0.05337467044591904, -0.016838211566209793, 0.024399409070611, 0.11527074128389359, 0.02456819824874401, -0.007382315117865801, 0.08041498064994812, 0.07927016913890839, 0.017217600718140602, 0.038696013391017914, -0.02245602197945118, 0.003428857075050473, -0.023958483710885048, 0.1580912321805954, 0.11539153009653091, 0.14533431828022003, 0.1810736209154129, 0.05129683390259743, -0.12306319922208786, -0.029403867200016975, -0.0500151664018631, 0.08510027080774307, 0.12531159818172455, 0.06925662606954575, 0.1815696358680725, -0.04741118475794792, -0.19575229287147522, 0.08531805872917175, 0.0018733828328549862, -0.14909613132476807, -0.07782984524965286, -0.054682061076164246, -0.09742926061153412, 0.024643104523420334, 0.11325778067111969, 0.019940707832574844, 0.015885736793279648, -0.006529635284096003, 0.09132573753595352, 0.00450444919988513, -0.02401844784617424, -0.07997357845306396, 0.01718944124877453, -0.03488682210445404, 0.3034706115722656, 0.08508702367544174, -0.13273383677005768, 0.10004150122404099, 0.08478537201881409, -0.007523915730416775, 0.36086833477020264, 0.1095677837729454, 0.3178050220012665, 0.31444740295410156, 0.035488855093717575, 0.07423056662082672, 0.12485962361097336, 0.10448698699474335, -0.05250062793493271, 0.2199762761592865, 0.09111926704645157, 0.003464725334197283, 0.06741940975189209, -0.055544741451740265, -0.05793166905641556, 0.018695050850510597, -0.017258061096072197, -0.0672779232263565, 0.0033355283085256815, -0.14251013100147247, -0.08235979825258255, -0.06682953983545303, 0.007229193113744259, -0.055985111743211746, 0.031089838594198227, 0.014068000949919224, 0.07067863643169403, 0.002018475439399481, 0.025119438767433167, 0.13880205154418945, -0.006040222942829132, 0.07454628497362137, 0.16187652945518494, -0.07231858372688293, -0.08415606617927551, 0.09056694060564041, 0.08077574521303177, 0.06108114868402481, -0.04115268215537071, -0.07779718190431595, 0.157216414809227, -0.011654279194772243, -0.2697732448577881, 0.024526482447981834, -0.10556153953075409, 0.0343443937599659, 0.0027804903220385313, -0.014694061130285263, 0.08477725088596344, -0.0769382119178772, 0.13063514232635498, -0.11452502012252808, 0.2197023332118988, 0.03549893572926521, 0.17861197888851166, 0.041139744222164154, -0.16133902966976166, 0.06675505638122559, -0.00905187614262104, 0.010002216324210167, -0.04765833541750908, 0.018270431086421013, -0.00858448538929224, 0.07336804270744324, 0.0394774004817009, 0.038943540304899216, 0.11231236904859543, -0.002102903788909316, 0.030398676171898842, -0.027190422639250755, -0.010176952928304672, -0.036323413252830505, -0.09182318300008774, 0.004438281524926424, 0.16973845660686493, 0.10921933501958847, -0.18555143475532532, 0.030024506151676178, 0.06398213654756546, 0.010346688330173492, 0.05533991754055023, 0.060839131474494934, 0.2310606837272644, -0.462793231010437, 0.010512403212487698, 0.04118813946843147, -0.1518353372812271, 0.2279430478811264, -0.4946650564670563, -0.10625208914279938, 0.03840767964720726, 0.005893384106457233, 0.03586317226290703, 0.007304093334823847, 0.04438040778040886, 0.0672202855348587, 0.007266608998179436, 0.15173283219337463, 0.08189643919467926, -0.02635239250957966, -0.1090376004576683, 0.009726164862513542, 0.11753956228494644, -0.0533156543970108, 0.056641485542058945, -0.0642005130648613, 0.010908303782343864, 0.013844726607203484, 0.001285551581531763, -0.05240599438548088, 0.012685611844062805, 0.018811343237757683, -0.0868372842669487, 0.004934500437229872, 0.13032963871955872, -0.03804830461740494, 0.006171979010105133, 0.02019748091697693, -0.022142386063933372, 0.010432806797325611, -0.11543907225131989, -0.0033682798966765404, 0.14690056443214417, -0.07871484011411667, -0.001267067389562726, -0.00923683401197195, 0.2075291872024536, -0.12900349497795105, -0.03550457954406738, 0.08442094922065735, -0.3068132698535919, -0.138373002409935, -0.19007468223571777, -0.061184413731098175, 0.030208829790353775, -0.22831928730010986, -0.022597339004278183, 0.08573885262012482, 0.008766965009272099, 0.017586098983883858, 0.005111623555421829, -0.0047611757181584835, 0.0027424453292042017, 0.092926986515522, -0.07423672080039978, -0.06920135021209717, -0.22281253337860107, -0.005090887192636728, 0.03644976019859314, 0.027220096439123154, -0.12946276366710663, 0.06310125440359116, -0.05266463756561279, -0.0351085364818573, 0.03720226138830185, 0.02106253243982792, -0.006388470996171236, 0.013272265903651714, 0.004373103380203247, 0.002471062820404768, 0.012379872612655163, 0.04394218325614929, -0.053225383162498474, 0.050206366926431656, 0.040920816361904144, -0.028209496289491653, 0.1321885734796524, 0.004856856074184179, 0.07346916198730469, 0.008750896900892258, 0.0507703498005867, -0.009539998136460781, -0.01952526718378067, -0.12417495995759964, -0.004625606816262007, -0.019748562946915627, 0.10737503319978714, -0.09269315749406815, 0.0514591708779335, -0.07012825459241867, 0.021994519978761673, -0.04062645882368088, 0.06895892322063446, -0.015550919808447361, -0.016758201643824577, 0.024237338453531265, 0.01013058889657259, 0.025168994441628456, -0.07154976576566696, 0.061569537967443466, -0.0016301710857078433, 0.056290943175554276, 0.10866367816925049, 0.0700349509716034, 0.10766670852899551, 0.20515970885753632, 0.06921687722206116, -0.004488249775022268, 0.00201522302813828, 0.01425462681800127, 0.0546112135052681, -0.002288257936015725, -0.04082952439785004, -0.04529828578233719, -0.11642234772443771, -0.03531666845083237, -0.014142773114144802, -0.0546661876142025, -0.09282539784908295, 0.008907352574169636, -0.06704774498939514, -0.0645899847149849, -0.026287002488970757, -0.0011186336632817984, -0.014677252620458603, 0.030429629608988762, 0.03851829469203949, 0.19442501664161682, -0.09693174064159393, -0.024728083983063698, 0.06723712384700775, -0.008964411914348602, -0.011063811369240284, 0.047246627509593964, 0.29918381571769714, -0.009952245280146599, 0.07558353990316391, 0.030741840600967407, 0.0243825763463974, -0.00829045008867979, 0.09563353657722473, 0.013650032691657543, -0.06716716289520264, -0.036978017538785934, -0.030953945592045784, -0.0023581129498779774, 0.19232229888439178, 0.002236573724076152, 0.019518272951245308, 0.019735295325517654, -0.029920348897576332, 0.2011176198720932, -0.0292696263641119, 0.01923223026096821, -0.05373818427324295, 0.006905499845743179, 0.012003553099930286, 0.006319114938378334, 0.013802816160023212, 0.137633815407753, 0.14114268124103546, -0.04374779388308525, 0.09204374253749847, 0.059122439473867416, -0.15111002326011658, -0.034872472286224365, -0.0062086693942546844, -0.0031748516485095024, 0.03549643233418465, -0.09192667156457901, 0.014974581077694893, 0.05166519060730934, 0.02357620932161808, 0.07522880285978317, -0.0014560738345608115, -0.005854273680597544, -0.11695965379476547, -0.04724137857556343, 0.1440524309873581, 0.05505932867527008, 0.00573133397847414, 0.0437886007130146, -0.14039334654808044, -0.08157329261302948, -0.09387002885341644, -0.035637155175209045, -0.04392101243138313, 0.06419125944375992, -0.03876033052802086, 0.008293460123240948, 0.0919722244143486, 0.0341385081410408, -0.1315184384584427, 0.02826027013361454, -0.006003207992762327, 0.11081897467374802, 0.0050383578054606915, 0.057261619716882706, 0.039518922567367554, 0.09283163398504257, -0.040434397757053375, 0.06508056074380875, -0.029538672417402267, -0.0071190521121025085, 0.13060948252677917, 0.007929791696369648, 0.01935708150267601, -0.10978356748819351, 0.038776565343141556, -0.08181315660476685, -0.06500256806612015, 0.012264692224562168, 0.055984169244766235, -0.0252766665071249, -0.03683993220329285, 0.006299285218119621, 0.009642924182116985, 0.00474341306835413, -0.010514526627957821, 0.01609257608652115, -0.005231794901192188, 0.024866988882422447, 0.003390286583453417, -0.05479232966899872, -0.050671905279159546, -0.08707145601511002, -0.008508305996656418, 0.18134663999080658, -0.08586402982473373, 0.09079630672931671, 0.1701999306678772, 0.057372745126485825, 0.03890211135149002, 0.19444061815738678, 0.11623232811689377, 0.01970021426677704, -0.09187138080596924, 0.05901405215263367, -0.15188677608966827, -0.07183356583118439, 0.02953292429447174, 0.039270732551813126, 0.01792513020336628, -0.004627366084605455, 0.08427257835865021, 0.12085095793008804, 0.025066055357456207, -0.06520556658506393, -0.024831509217619896, 0.03176439553499222, -0.12258440256118774, 0.03272413834929466, -0.07555592805147171, 0.14407406747341156, 0.1446923166513443, 0.10910509526729584, -0.08697526156902313, -0.00474596256390214, -0.008259710855782032, 0.18126201629638672, -0.13997386395931244, -0.12934428453445435, -0.17425045371055603, -0.14088840782642365, -0.22505496442317963, 0.05973941087722778, 0.17031173408031464, -0.11067971587181091, 0.03319639340043068, 0.016970541328191757, 0.0042065465822815895, -0.027092477306723595, 0.06977192312479019, 0.025107184424996376, -0.11053600162267685, -0.18516530096530914, 0.07550688087940216, -0.10377872735261917, -0.18386921286582947, 0.005991897080093622, -0.15819773077964783, 0.013619054108858109, 0.04844582825899124, -0.015447720885276794, 0.10991355031728745, 0.043648384511470795, -0.11273067444562912, 0.15656986832618713, 0.15554897487163544, -0.002826593816280365, -0.038845401257276535, 0.15236026048660278, 0.01653599552810192, -0.10471045225858688, -0.3206518292427063, 0.053577180951833725, -0.07985322922468185, 0.12786950170993805, -0.06242319941520691, 0.07606039941310883, 0.011703354306519032, -0.008322997950017452, -0.008699114434421062, -0.13111060857772827, 0.023725442588329315, 0.01726129837334156, 0.05876057222485542, -0.03321985527873039, 0.05502607673406601, 0.030017070472240448, -0.034821778535842896, 0.044721297919750214, 0.101694256067276, 0.049289338290691376, -0.05830397829413414, 0.020656801760196686, 0.009954153560101986, -0.044627171009778976, -0.07788107544183731, -0.17100536823272705, 0.02877037040889263, 0.019905032590031624, -0.07708911597728729, -0.02289334125816822, 0.029993321746587753, 0.012730294838547707, 0.12136353552341461, -0.07719119638204575, 0.07883642613887787, 0.05577106028795242, -0.0043141101486980915, 0.03290349245071411, 0.01848907582461834, 0.002715120557695627, -0.043140869587659836, 0.07655049860477448, -0.005482841283082962, -0.01561190839856863, -0.03664609044790268, 0.0021732687018811703, -0.008302069269120693, 0.19481432437896729, 0.10706984251737595, -0.15098020434379578, 0.10473863780498505, 0.35678112506866455, -0.009508016519248486, 0.013248742558062077, 0.19056639075279236, -0.09157676249742508, -0.2193361073732376, -0.04526733607053757, -0.07000761479139328, 0.0390738807618618, -0.006956012453883886, 0.10561531782150269, 0.049464885145425797, -0.050710927695035934, -0.08128220587968826, 0.028920501470565796, -0.019102267920970917, -0.055440936237573624, -0.03535989671945572, 0.12063788622617722, -0.012214797548949718, 0.09161433577537537, 0.06021915748715401, -0.02321092039346695, 0.07624145597219467, -0.18161052465438843, 0.0336027666926384, -0.08424509316682816, -0.006771375425159931, -0.0019076341995969415, 0.03816232457756996, -0.0007744910544715822, 0.012812202796339989, 0.047536592930555344, -0.03160211071372032, -0.058715786784887314, -0.008489634841680527, -0.061000291258096695, -0.006820277310907841, -0.0044312430545687675, 0.04566909000277519, -0.006699971854686737, 0.09529705345630646, -0.07450015842914581, 0.02662116102874279, -0.025414837524294853, 0.08198850601911545, 0.07451725751161575, 0.11426648497581482, 0.05965487286448479, -0.0970940813422203, -0.03631693497300148, -0.013885808177292347, -0.045866385102272034, 0.16290400922298431, -0.02889004908502102, 0.1161700040102005, 0.08739493787288666, -0.11351096630096436, -0.06062677875161171, 0.10772960633039474, -0.02560640685260296, 0.07751680910587311, -0.10845332592725754, 0.0019068397814407945, -0.07534290850162506, -0.08204468339681625, 0.16656216979026794, -0.05021122470498085, 0.12110895663499832, 0.008551867678761482, -0.05674958601593971, -0.19170068204402924, -0.02665233425796032, -0.03275732323527336, 0.032205771654844284, -0.005003427155315876, -0.13141795992851257, 0.04435361921787262, -0.035311441868543625, 0.051841653883457184, 0.019225439056754112, 0.06870316714048386, -0.0392429381608963, -0.10101300477981567, 0.010844998061656952, 0.0728413462638855, -0.06427573412656784, -0.0514608696103096, 0.07085778564214706, -0.02378981187939644, 0.0016854465939104557, 0.07792896777391434, 0.03720853850245476, -0.059667181223630905, -0.07644010335206985, 0.02163614146411419, -0.027017662301659584, -0.003088119439780712, -0.0989667996764183, -0.03913822025060654, -0.07596976310014725, 0.02590310573577881, 0.017715467140078545, -0.03835783898830414, 0.059145208448171616, 0.04517063498497009, -0.14288750290870667, 0.000748267921153456, 0.013570395298302174, -0.10760839283466339, 0.04580095410346985, -0.037626199424266815, -0.055409662425518036, 0.05307552590966225, 0.0603635199368, -0.022689882665872574, 0.0037790348287671804, 0.07587698847055435, -0.04435175284743309, 0.2617245018482208, -0.008662134408950806, 0.09927282482385635, -0.09097731858491898, 0.023190073668956757, -0.06010225787758827, 0.10265494138002396, 0.032572973519563675, 0.08763974905014038, 0.071159228682518, -0.08318590372800827, -0.01961914449930191, -0.0018052058294415474, 0.004678540397435427, -0.0264047309756279, -0.014519975520670414, -0.057559046894311905, 0.024934660643339157, 0.08651409298181534, -0.049981169402599335, 0.029489658772945404, -0.006767811719328165, 0.021165814250707626, -0.04576762020587921, -0.07989151030778885, -0.08270293474197388, 0.017666630446910858, 0.04982709512114525, -0.05640736594796181, 0.03232499212026596, -0.02023754082620144, 0.03288915380835533};

const float bias_raw[16]={0.3846295475959778, 0.9347521662712097, 0.5503185391426086, 0.09094804525375366, 0.8423282504081726, 0.5404276251792908, 0.560431182384491, 0.7526561617851257, 0.4364055097103119, 0.6519505381584167, 0.10853563994169235, 0.5392001271247864, 0.7242470383644104, 1.0230261087417603, 0.5287861227989197, 0.48794910311698914};

const int stride_width=1;
const int stride_height=1;
const TfLiteFusedActivation activation=kTfLiteActRelu;
const int dilation_width_factor=1;
const int dilation_height_factor=1;
const int32_t filter_input_channel=128;
const int32_t filter_output_channel=16;
const int filter_height=1;
const int filter_width=1;
const int filter_dims_size=4;
const int32_t filter_dims_raw[4]={16,1,1,128};
const int bias_dims_size=1;
const int32_t bias_dims_raw[1]={16};
const TfLitePadding paddings=kTfLitePaddingSame;
const TfLiteType filter_type=kTfLiteFloat32;
const bool data_supports_multithreaded_kernel=true;

struct OpData {
  // IDs are the arbitrary identifiers used by TF Lite to identify and access
  // memory buffers.
  int im2col_id = kTensorNotAllocated;
  int hwcn_weights_id = kTensorNotAllocated;
  int input_quantized_id = kTensorNotAllocated;
  int scaling_factors_id = kTensorNotAllocated;
  int input_offset_id = kTensorNotAllocated;
  int accum_scratch_id = kTensorNotAllocated;
  // Row sums are used to cache filter sums for hybrid zero-point calculations.
  int row_sums_id = kTensorNotAllocated;

  TfLitePaddingValues padding;
  // The scaling factor from input to output (aka the 'real multiplier') can
  // be represented as a fixed point multiplier plus a left shift.
  int32_t output_multiplier;
  int output_shift;

  // Per channel output multiplier and shift.
  std::vector<int32_t> per_channel_output_multiplier;
  std::vector<int> per_channel_output_shift;

  // The range of the fused activation layer. For example for kNone and
  // uint8_t these would be 0 and 255.
  int32_t output_activation_min;
  int32_t output_activation_max;
  // Indexes are the offset to the memory buffer in the array used to keep track
  // of the allocated temporaries.
  int32_t im2col_index;
  int32_t hwcn_weights_index;
  int32_t input_quantized_index;
  int32_t scaling_factors_index;
  int32_t accum_scratch_index;
  int32_t input_offset_index;
  int32_t row_sums_index;

  bool need_hwcn_weights = false;
  bool have_weights_been_transposed = false;
  bool need_im2col = false;
  // If it's true, it means im2col is needed but gets disabled because the
  // temporary im2col tensor requires too much memory (i.e.
  // >= kMaxIm2colBufferSize);
  bool im2col_oversized = false;

  bool supports_multithreaded_kernel = false;
  bool is_hybrid_per_channel = false;
  bool compute_hybrid_row_sums = true;

  // Number of convolution groups.
  int32_t groups = 1;
};

inline PaddingType RuntimePaddingType(TfLitePadding padding) {
  switch (padding) {
    case TfLitePadding::kTfLitePaddingSame:
      return PaddingType::kSame;
    case TfLitePadding::kTfLitePaddingValid:
      return PaddingType::kValid;
    case TfLitePadding::kTfLitePaddingUnknown:
    default:
      return PaddingType::kNone;
  }
}

void* Init(TfLiteContext* context, const char* buffer, size_t length) {
  // This is a builtin op, so we don't use the contents in 'buffer', if any.
  // Instead, we allocate a new object to use as scratch space for im2col, and
  // to carry information from Prepare() to Eval().
  auto* data = new OpData;
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
  eigen_support::IncrementUsageCounter(context);
#endif
  return data;
}

void Free(TfLiteContext* context, void* buffer) {
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
  eigen_support::DecrementUsageCounter(context);
#endif
  delete reinterpret_cast<OpData*>(buffer);
}

// Naive implementation of transpose for floats. Could be optimized to be more
// cache friendly, but for now it's a one-time cost on first run, and we would
// prefer to remove the need to do this at all eventually.
void TransposeFloatTensor(const TfLiteTensor* input, TfLiteTensor* output) {
  const int rows = output->dims->data[1];
  const int cols = output->dims->data[0];
  // const float* input_data = GetTensorData<float>(input);
  const float* input_data = filter_raw;
  float* output_data = GetTensorData<float>(output);
  for (int i = 0; i < rows; ++i) {
    for (int j = 0; j < cols; ++j) {
      const float in_value = input_data[i * cols + j];
      output_data[j * rows + i] = in_value;
    }
  }
}

// Check if im2col needs to be allocated, as some version of optimized Conv dont
// use it. If any change is supporting im2col in any of the Conv versions, then
// it should be updated here as well
bool IsIm2ColRequired(const TfLiteTensor* input, TfLiteConvParams* params,
                      const TfLiteTensor* filter, OpData* data, bool is_hybrid,
                      KernelType kernel_type) {
  // If HWCN weights are required, Im2Col not required
  if (data->need_hwcn_weights) return false;

  // segregate based on dilated conv & non-dialated conv
  const bool need_dilated_im2col =
      params->dilation_width_factor != 1 || params->dilation_height_factor != 1;
  // const bool need_non_dilated_im2col =
  //     params->stride_width != 1 || params->stride_height != 1 ||
  //     filter->dims->data[2] != 1 || filter->dims->data[1] != 1;
  const bool need_non_dilated_im2col =
      stride_width != 1 || stride_height != 1 ||
      filter_width != 1 || filter_height != 1;

  const bool need_im2col = need_dilated_im2col || need_non_dilated_im2col;

  // Return early as basic requirement is not met
  if (!need_im2col) return false;

  // Special case for Hybrid, as it supports only non-dilated im2col currently
  const bool is_hybrid_non_dilated = is_hybrid && need_non_dilated_im2col;
  const bool is_quantized = input->type == kTfLiteUInt8 ||
                            input->type == kTfLiteInt8 ||
                            input->type == kTfLiteInt16;

  switch (kernel_type) {
    case kReference:
      if (is_hybrid) {
        return true;
      } else {
        return false;
      }
    case kGenericOptimized:
    case kCblasOptimized:
      if (is_hybrid && !need_non_dilated_im2col) {
        return false;
      } else {
        return true;
      }
    case kMultithreadOptimized:
      if (is_hybrid_non_dilated || is_quantized ||
          !data->supports_multithreaded_kernel) {
        return true;
      } else {
        return false;
      }
    default:
      return false;
  }
}

// Allocate temporary tensors (`im2col`, `hwcn_weights` if necessary).
// Note: `context->AddTensors` might invalidate pointers to existing tensors.
// Therefore the logic to add tensors are isolated into this function.
static TfLiteStatus AllocateTemporaryTensorsIfRequired(
    TfLiteContext* context, TfLiteNode* node, bool is_hybrid,
    bool is_per_channel, KernelType kernel_type, size_t im2col_bytes) {
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  TfLiteConvParams* params;
  OpData* data = reinterpret_cast<OpData*>(node->user_data);

  // TF_LITE_ENSURE(context, node->inputs->size >= 2);
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  const TfLiteTensor* filter;
  // TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 1, &filter));

  // If we're using the optimized multithreaded EigenTensor implementation of
  // convolution, it expects the filter weights to be transposed compared to
  // the normal TF Lite buffer format. Typical TF Lite weights are
  // [filter_count, filter_height, filter_width, input_depth], but for the float
  // implementation we need them as [filter_height, filter_width, input_depth,
  // filter_count]. We get to that format by transposing, and create a temporary
  // buffer to store the results.
  // This path is only used for float processing, so only create the buffer if
  // we're running with that data type.
  data->need_hwcn_weights =
      input->type == kTfLiteFloat32 && data->supports_multithreaded_kernel;

  // We don't always need to allocate im2col. It is only used in some versions
  // of the optimized Conv. This test just mimics something that happens inside
  // optimized_ops.h, in order to avoid a DCHECK(!im2col_data).
  data->need_im2col =
      IsIm2ColRequired(input, params, filter, data, is_hybrid, kernel_type);

  // If im2col_oversized is found to be true, we have to fallback to an
  // execution path (like kReference in float/quantized cases) that doesn't
  // require im2col operation. Therefore, we have to skip checking the hybrid
  // case (but not the hybrid-per-channel one) where there's no such a fallback
  // execution path.
  // TODO(b/178743262): Consider making this check conditioned on the available
  // memory of the system, rather than coupling to the mobile platform check.
  if (IsMobilePlatform() && !(is_hybrid && !is_per_channel) &&
      data->need_im2col && im2col_bytes >= kMaxIm2colBufferSizeMobile) {
    data->need_im2col = false;
    data->im2col_oversized = true;
  }
  int temporaries_count = 0;
  if (data->need_im2col) {
    data->im2col_index = temporaries_count;
    if (data->im2col_id == kTensorNotAllocated) {
      context->AddTensors(context, 1, &data->im2col_id);
    }
    ++temporaries_count;
  }
  if (data->need_hwcn_weights) {
    data->hwcn_weights_index = temporaries_count;
    if (data->hwcn_weights_id == kTensorNotAllocated) {
      context->AddTensors(context, 1, &data->hwcn_weights_id);
    }
    ++temporaries_count;
  }

  if (is_hybrid) {
    // Allocate tensor to store the on-the-fly quantized inputs.
    data->input_quantized_index = temporaries_count;
    if (data->input_quantized_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->input_quantized_id));
    }
    ++temporaries_count;

    // Allocate tensor to store the quantization params computed during
    // on-the-fly input quantization.
    data->scaling_factors_index = temporaries_count;
    if (data->scaling_factors_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->scaling_factors_id));
    }
    ++temporaries_count;

    // Allocate tensor to store the accumulators for the matrix multiply.
    data->accum_scratch_index = temporaries_count;
    if (data->accum_scratch_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->accum_scratch_id));
    }
    ++temporaries_count;
    if (is_per_channel) {
      data->input_offset_index = temporaries_count;
      if (data->input_offset_id == kTensorNotAllocated) {
        TF_LITE_ENSURE_OK(
            context, context->AddTensors(context, 1, &data->input_offset_id));
      }
      ++temporaries_count;

      data->row_sums_index = temporaries_count;
      if (data->row_sums_id == kTensorNotAllocated) {
        TF_LITE_ENSURE_OK(context,
                          context->AddTensors(context, 1, &data->row_sums_id));
      }
      ++temporaries_count;
    }
  }

  TfLiteIntArrayFree(node->temporaries);
  node->temporaries = TfLiteIntArrayCreate(temporaries_count);

  return kTfLiteOk;
}

TfLiteStatus Prepare(KernelType kernel_type, TfLiteContext* context,
                     TfLiteNode* node) {
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  // TfLiteConvParams* params;
  // TfLitePadding paddings=kTfLitePaddingSame;
  // --------------------------------------------------------------------------
  // int stride_width=2;
  // int stride_height=2;
  // TfLiteFusedActivation activation=kTfLiteActRelu6;
  // int dilation_width_factor = 1;
  // int dilation_height_factor = 1;
  // const int32_t filter_input_channel = 3;
  // const int32_t filter_output_channel = 24;
  // int filter_height = 3;
  // int filter_width = 3;
  // TfLiteType filter_type = kTfLiteFloat32;
  // const bool data_supports_multithreaded_kernel=true;
  // --------------------------------------------------------------------------
  OpData* data = reinterpret_cast<OpData*>(node->user_data);

  bool has_bias = false;
  // bool has_bias = 0;
  // Check number of inputs/outputs
  // TF_LITE_ENSURE(context, has_bias || node->inputs->size == 2);
  TF_LITE_ENSURE_EQ(context, node->outputs->size, 1);
  TfLiteTensor* output;
  TF_LITE_ENSURE_OK(context, GetOutputSafe(context, node, 0, &output));
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  const TfLiteTensor* filter;
  // TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 1, &filter));

  // Check dimensionality of input, filter
  TF_LITE_ENSURE_EQ(context, input->dims->size, 4);
  // TF_LITE_ENSURE_EQ(context, filter->dims->size, 4);
  TF_LITE_ENSURE_EQ(context, filter_dims_size, 4);
  
  // Check input channels matching filter
  // Filter input channel can be a factor of channels of input (grouped conv)
  // or equals (normal conv).
  auto input_channel = input->dims->data[3];
  // auto filter_input_channel = filter->dims->data[3];

  TF_LITE_ENSURE_EQ(context, input_channel % filter_input_channel, 0);
  data->groups = input_channel / filter_input_channel;

  // Check types. (We assume that UINT8 refers to quantized tensors)
  TfLiteType input_type = input->type;
  TF_LITE_ENSURE(context,
                 input_type == kTfLiteFloat32 || input_type == kTfLiteUInt8 ||
                     input_type == kTfLiteInt8 || input_type == kTfLiteInt16);
  TF_LITE_ENSURE_TYPES_EQ(context, output->type, input_type);

  if (input_type == kTfLiteInt16) {
    TF_LITE_ENSURE_EQ(context, input->params.zero_point, 0);
    TF_LITE_ENSURE_EQ(context, output->params.zero_point, 0);
  }
  // Filter must have zero zero-points in per-channel quantization.
  if (input_type == kTfLiteInt16 || input_type == kTfLiteInt8) {
    TF_LITE_ENSURE_EQ(context, filter->quantization.type,
                      kTfLiteAffineQuantization);
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    for (int i = 0; i < affine_quantization->zero_point->size; ++i) {
      TF_LITE_ENSURE_EQ(context, affine_quantization->zero_point->data[i], 0);
    }
  }

  const TfLiteTensor* bias = nullptr;
  // std::cout << "codes runs here #-2" << std::endl;
  // TODO(ahentz): At this point the optimized versions require 'bias'. We can
  // either change that or document that convolution requires it.

  // TF_LITE_ENSURE(context, has_bias);

  if (has_bias) {
    TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 2, &bias));
    if (input_type == kTfLiteUInt8 || input_type == kTfLiteInt8) {
      TF_LITE_ENSURE_TYPES_EQ(context, bias->type, kTfLiteInt32);
      TF_LITE_ENSURE_EQ(context, bias->params.zero_point, 0);
    } else if (input_type == kTfLiteInt16) {
      TF_LITE_ENSURE(context, (bias->type == kTfLiteInt32) ||
                                  (bias->type == kTfLiteInt64));
      TF_LITE_ENSURE_EQ(context, bias->params.zero_point, 0);
    } else {
      TF_LITE_ENSURE_TYPES_EQ(context, bias->type, input_type);
    }
    TF_LITE_ENSURE_EQ(context, NumElements(bias), SizeOfDimension(filter, 0));
  }

  const bool is_hybrid =
      (input->type == kTfLiteFloat32 &&
       (filter_type == kTfLiteUInt8 || filter_type == kTfLiteInt8));

  if (is_hybrid && filter_type == kTfLiteInt8 &&
      filter->quantization.type == kTfLiteAffineQuantization &&
      filter->quantization.params &&
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params)
          ->scale &&
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params)
              ->scale->size > 1) {
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    const float scale = affine_quantization->scale->data[0];
    for (int i = 1; i < affine_quantization->scale->size; i++) {
      if (affine_quantization->scale->data[i] != scale) {
        data->is_hybrid_per_channel = true;
        break;
      }
    }
  }
  //----------------------------------------------------------------------------
  // std::cout << "codes runs here #-1" << std::endl;
  // The multi-threaded kernel supports neither dilation nor hybrid kernels, and
  // is incompatible with mutable input filters that might change between evals.
  // data->supports_multithreaded_kernel =
  //     (kernel_type == kMultithreadOptimized) &&
  //     (context->recommended_num_threads != 1) && !is_hybrid &&
  //     (dilation_width_factor == 1) &&
  //     (dilation_height_factor == 1) &&
  //     (filter->allocation_type != kTfLiteArenaRw) && !IsDynamicTensor(filter);
  data->supports_multithreaded_kernel = data_supports_multithreaded_kernel;
  // const char * bool_value = data->supports_multithreaded_kernel ? "true" : "false";
  // std::cout << bool_value << std::endl;    
  // std::cout << "codes runs here #-1.1" << std::endl;

  
  // int channels_in = filter->dims->data[3];
  int channels_in = filter_input_channel;
  // int channels_out = filter->dims->data[0];
  int channels_out = filter_output_channel;
  int width = input->dims->data[2];
  int height = input->dims->data[1];
  // int filter_width = filter->dims->data[2];
  // int filter_height = filter->dims->data[1];
  int batches = input->dims->data[0];
  //----------------------------------------------------------------------------
  // std::cout << "codes runs here #-1.2" << std::endl;
  // Matching GetWindowedOutputSize in TensorFlow.
  auto padding = paddings;
  int out_width, out_height;
  data->padding = ComputePaddingHeightWidth(
      stride_height, stride_width,
      dilation_height_factor, dilation_width_factor, height,
      width, filter_height, filter_width, padding, &out_height, &out_width);
  // std::cout << "codes runs here #-1.2" << std::endl;
  size_t im2col_type_size;
  TF_LITE_ENSURE_STATUS(GetSizeOfType(context, input->type, &im2col_type_size));
  // Note that we intentionally promote the first multiplicand (i.e. 'batches')
  // to 'size_t' to avoid integer overflow here.
  const size_t im2col_bytes = static_cast<size_t>(batches) * out_height *
                              out_width * channels_in * filter_height *
                              filter_width * im2col_type_size;
  TF_LITE_ENSURE_STATUS(AllocateTemporaryTensorsIfRequired(
      context, node, is_hybrid, data->is_hybrid_per_channel, kernel_type,
      im2col_bytes));
  // std::cout << "codes runs here #0" << std::endl;
  // TF_LITE_ENSURE(context, has_bias);

  // Note that full fixed-point inference requires that all tensors have their
  // parameters set. This is usually done during quantized training or
  // calibration.
  if (input_type != kTfLiteFloat32) {
    TF_LITE_ENSURE_EQ(context, filter->quantization.type,
                      kTfLiteAffineQuantization);
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    TF_LITE_ENSURE(context, affine_quantization);
    TF_LITE_ENSURE(context, affine_quantization->scale);
    TF_LITE_ENSURE(context, (affine_quantization->scale->size == 1 ||
                             affine_quantization->scale->size == channels_out));

    data->per_channel_output_multiplier.resize(channels_out);
    data->per_channel_output_shift.resize(channels_out);
    // TF_LITE_ENSURE_STATUS(tflite::PopulateConvolutionQuantizationParams(
    //     context, input, filter, bias, output, activation,
    //     &data->output_multiplier, &data->output_shift,
    //     &data->output_activation_min, &data->output_activation_max,
    //     data->per_channel_output_multiplier.data(),
    //     data->per_channel_output_shift.data(), channels_out));
  }
  // std::cout << "codes runs here #0.1" << std::endl;
  TfLiteIntArray* output_size = TfLiteIntArrayCreate(4);
  output_size->data[0] = batches;
  output_size->data[1] = out_height;
  output_size->data[2] = out_width;
  output_size->data[3] = channels_out;
  auto output_status = context->ResizeTensor(context, output, output_size);

  if (output_status != kTfLiteOk) return output_status;

  if (data->need_im2col) {
    node->temporaries->data[data->im2col_index] = data->im2col_id;

    TfLiteIntArray* im2col_size = TfLiteIntArrayCreate(4);

    // auto filter_input_channel = filter->dims->data[3];
    im2col_size->data[0] = output_size->data[0];
    im2col_size->data[1] = output_size->data[1];
    im2col_size->data[2] = output_size->data[2];
    im2col_size->data[3] = filter_input_channel * filter_height * filter_width;

    TfLiteTensor* im2col =
        &context->tensors[node->temporaries->data[data->im2col_index]];
    im2col->type = input->type;
    if (is_hybrid) {
      im2col->type = filter_type;
    }
    im2col->allocation_type = kTfLiteArenaRw;
    auto im2col_status = context->ResizeTensor(context, im2col, im2col_size);
    if (im2col_status != kTfLiteOk) return im2col_status;
  }
  // std::cout << "codes runs here #0.2" << std::endl;
  if (data->need_hwcn_weights) {
    node->temporaries->data[data->hwcn_weights_index] = data->hwcn_weights_id;
    TfLiteIntArray* hwcn_weights_size = TfLiteIntArrayCreate(2);

    // Because we're treating the filter weights as a matrix when we do the
    // transpose, we allocate the buffer with a two-dimensional shape, where one
    // dimension is the number of elements in each filter, and the second is the
    // total number of filters.
    // auto filter_input_channel = filter->dims->data[3];
    hwcn_weights_size->data[0] =
        (filter_height * filter_width * filter_input_channel);
    hwcn_weights_size->data[1] = channels_out;

    TfLiteTensor* hwcn_weights =
        &context->tensors[node->temporaries->data[data->hwcn_weights_index]];
    hwcn_weights->type = input_type;
    hwcn_weights->allocation_type = kTfLiteArenaRwPersistent;

    auto hwcn_weights_status =
        context->ResizeTensor(context, hwcn_weights, hwcn_weights_size);
    if (hwcn_weights_status != kTfLiteOk) return hwcn_weights_status;

    // TODO(petewarden): If Resize() is called when the size hasn't actually
    // changed, this will do extra redundant work.
    data->have_weights_been_transposed = false;
  }
  // std::cout << "codes runs here #0.3" << std::endl;
  if (is_hybrid) {
    node->temporaries->data[data->input_quantized_index] =
        data->input_quantized_id;
    TfLiteTensor* input_quantized;
    TF_LITE_ENSURE_OK(
        context, GetTemporarySafe(context, node, data->input_quantized_index,
                                  &input_quantized));
    input_quantized->type = kTfLiteInt8;
    input_quantized->allocation_type = kTfLiteArenaRw;
    if (!TfLiteIntArrayEqual(input_quantized->dims, input->dims)) {
      TfLiteIntArray* input_quantized_size = TfLiteIntArrayCopy(input->dims);
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, input_quantized,
                                                       input_quantized_size));
    }
    // std::cout << "codes runs here #0.4" << std::endl;
    node->temporaries->data[data->scaling_factors_index] =
        data->scaling_factors_id;
    TfLiteTensor* scaling_factors;
    TF_LITE_ENSURE_OK(
        context, GetTemporarySafe(context, node, data->scaling_factors_index,
                                  &scaling_factors));
    scaling_factors->type = kTfLiteFloat32;
    scaling_factors->allocation_type = kTfLiteArenaRw;
    // Only one scale factor per batch is typically necessary. See optimized
    // implementation for why we need to allocate for the height of the inputs
    // flattened to 2D.
    TF_LITE_ENSURE(context, channels_in != 0);
    const int height = NumElements(input) / channels_in;
    int scaling_dims[1] = {height};
    if (!TfLiteIntArrayEqualsArray(scaling_factors->dims, 1, scaling_dims)) {
      TfLiteIntArray* scaling_factors_size = TfLiteIntArrayCreate(1);
      scaling_factors_size->data[0] = height;
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, scaling_factors,
                                                       scaling_factors_size));
    }
    // std::cout << "codes runs here #0.5" << std::endl;
    node->temporaries->data[data->accum_scratch_index] = data->accum_scratch_id;
    TfLiteTensor* accum_scratch;
    TF_LITE_ENSURE_OK(context,
                      GetTemporarySafe(context, node, data->accum_scratch_index,
                                       &accum_scratch));
    accum_scratch->type = kTfLiteInt32;
    accum_scratch->allocation_type = kTfLiteArenaRw;
    const int scratch_width = batches * out_height * out_width;
    int accum_scratch_dims[2] = {channels_out, scratch_width};
    if (!TfLiteIntArrayEqualsArray(accum_scratch->dims, 2,
                                   accum_scratch_dims)) {
      TfLiteIntArray* accum_scratch_size = TfLiteIntArrayCreate(2);
      accum_scratch_size->data[0] = channels_out;
      accum_scratch_size->data[1] = scratch_width;
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, accum_scratch,
                                                       accum_scratch_size));
    }
    // std::cout << "codes runs here #0.6" << std::endl;
    if (data->is_hybrid_per_channel) {
      const auto* affine_quantization =
          reinterpret_cast<TfLiteAffineQuantization*>(
              filter->quantization.params);
      TF_LITE_ENSURE_EQ(
          context, affine_quantization->scale->size,
          filter->dims->data[affine_quantization->quantized_dimension]);
      node->temporaries->data[data->input_offset_index] = data->input_offset_id;
      TfLiteTensor* input_offsets;
      TF_LITE_ENSURE_OK(
          context, GetTemporarySafe(context, node, data->input_offset_index,
                                    &input_offsets));
      input_offsets->type = kTfLiteInt32;
      input_offsets->allocation_type = kTfLiteArenaRw;
      // See above comment for the need to allocate for height of inputs.
      TF_LITE_ENSURE(context, channels_in != 0);
      const int height = NumElements(input) / channels_in;
      const int input_offset_dims[1] = {height};
      if (!TfLiteIntArrayEqualsArray(input_offsets->dims, 1,
                                     input_offset_dims)) {
        TfLiteIntArray* input_offsets_size = TfLiteIntArrayCreate(1);
        input_offsets_size->data[0] = input_offset_dims[0];
        TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, input_offsets,
                                                         input_offsets_size));
      }
      node->temporaries->data[data->row_sums_index] = data->row_sums_id;
      TfLiteTensor* row_sums;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->row_sums_index, &row_sums));
      row_sums->type = kTfLiteInt32;
      row_sums->allocation_type = kTfLiteArenaRwPersistent;
      // See above comment for the need to allocate for height of inputs.
      const int row_sums_dims[1] = {channels_out};
      if (!TfLiteIntArrayEqualsArray(row_sums->dims, 1, row_sums_dims)) {
        TfLiteIntArray* row_sums_size = TfLiteIntArrayCreate(1);
        row_sums_size->data[0] = row_sums_dims[0];
        TF_LITE_ENSURE_OK(
            context, context->ResizeTensor(context, row_sums, row_sums_size));
      }
    }
  }
  // std::cout << "codes runs here #1" << std::endl;
  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus Prepare(TfLiteContext* context, TfLiteNode* node) {
  return Prepare(kernel_type, context, node);
}

template <KernelType kernel_type>
void EvalQuantized(TfLiteContext* context, TfLiteNode* node,
                   TfLiteConvParams* params, OpData* data,
                   const TfLiteTensor* input, const TfLiteTensor* filter,
                   const TfLiteTensor* bias, TfLiteTensor* im2col,
                   TfLiteTensor* output) {
  auto input_offset = -input->params.zero_point;
  auto filter_offset = -filter->params.zero_point;
  auto output_offset = output->params.zero_point;

  KernelType effective_kernel_type;
  if ((kernel_type == kMultithreadOptimized ||
       kernel_type == kCblasOptimized) &&
      (params->dilation_width_factor != 1 ||
       params->dilation_height_factor != 1)) {
    // kMultithreadOptimized and kCblasOptimized do not support dilation.
    // Therefore, fallback to optimized.
    effective_kernel_type = kGenericOptimized;
  } else {
    effective_kernel_type = kernel_type;
  }

  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = PaddingType::kSame;
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.dilation_width_factor = dilation_width_factor;
  op_params.dilation_height_factor = dilation_height_factor;
  op_params.stride_width = stride_width;
  op_params.stride_height = stride_height;
  op_params.input_offset = input_offset;
  op_params.weights_offset = filter_offset;
  op_params.output_offset = output_offset;
  op_params.output_multiplier = data->output_multiplier;
  op_params.output_shift = -data->output_shift;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;
  switch (effective_kernel_type) {
    case kReference: {
      reference_ops::Conv(
          op_params, GetTensorShape(input), GetTensorData<uint8_t>(input),
          GetTensorShape(filter), GetTensorData<uint8_t>(filter),
          GetTensorShape(bias), GetTensorData<int32_t>(bias),
          GetTensorShape(output), GetTensorData<uint8_t>(output),
          GetTensorShape(im2col), GetTensorData<uint8_t>(im2col),
          /* cpu_backend_context = */ nullptr);
      break;
    }
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      // There is only one optimized implementation for Quantized Conv.
      optimized_ops::Conv(
          op_params, GetTensorShape(input), GetTensorData<uint8_t>(input),
          GetTensorShape(filter), GetTensorData<uint8_t>(filter),
          GetTensorShape(bias), GetTensorData<int32_t>(bias),
          GetTensorShape(output), GetTensorData<uint8_t>(output),
          GetTensorShape(im2col), GetTensorData<uint8_t>(im2col),
          CpuBackendContext::GetFromContext(context));
      break;
    }
  }
}

template <KernelType kernel_type>
void EvalQuantizedPerChannel(TfLiteContext* context, TfLiteNode* node,
                             TfLiteConvParams* params, OpData* data,
                             const TfLiteTensor* input,
                             const TfLiteTensor* filter,
                             const TfLiteTensor* bias, TfLiteTensor* output,
                             TfLiteTensor* im2col) {
  ConvParams op_params;
  op_params.input_offset = -input->params.zero_point;
  op_params.output_offset = output->params.zero_point;
  op_params.stride_height = stride_height;
  op_params.stride_width = stride_width;
  op_params.dilation_height_factor = dilation_height_factor;
  op_params.dilation_width_factor = dilation_width_factor;
  op_params.padding_values.height = data->padding.height;
  op_params.padding_values.width = data->padding.width;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  switch (effective_kernel_type) {
    case kReference: {
      reference_integer_ops::ConvPerChannel(
          op_params, data->per_channel_output_multiplier.data(),
          data->per_channel_output_shift.data(), GetTensorShape(input),
          GetTensorData<int8>(input), GetTensorShape(filter),
          GetTensorData<int8>(filter), GetTensorShape(bias),
          GetTensorData<int32>(bias), GetTensorShape(output),
          GetTensorData<int8>(output));
      break;
    }
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      optimized_integer_ops::ConvPerChannel(
          op_params, data->per_channel_output_multiplier.data(),
          data->per_channel_output_shift.data(), GetTensorShape(input),
          GetTensorData<int8>(input), GetTensorShape(filter),
          GetTensorData<int8>(filter), GetTensorShape(bias),
          GetTensorData<int32>(bias), GetTensorShape(output),
          GetTensorData<int8>(output), GetTensorShape(im2col),
          GetTensorData<int8>(im2col),
          CpuBackendContext::GetFromContext(context));
      break;
    }
  }
}

template <KernelType kernel_type>
void EvalQuantizedPerChannel16x8(TfLiteContext* context, TfLiteNode* node,
                                 TfLiteConvParams* params, OpData* data,
                                 const TfLiteTensor* input,
                                 const TfLiteTensor* filter,
                                 const TfLiteTensor* bias, TfLiteTensor* output,
                                 TfLiteTensor* im2col) {
  ConvParams op_params;
  op_params.input_offset = -input->params.zero_point;
  op_params.output_offset = output->params.zero_point;
  op_params.stride_height = stride_height;
  op_params.stride_width = stride_width;
  op_params.dilation_height_factor = dilation_height_factor;
  op_params.dilation_width_factor = dilation_width_factor;
  op_params.padding_values.height = data->padding.height;
  op_params.padding_values.width = data->padding.width;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  // To prevent 32bit accum overflow for 16x8 quantization, it enables the
  // optimized path only when zero_point is 0.
  bool has_non_zero_point = input->params.zero_point ||
                            filter->params.zero_point ||
                            output->params.zero_point;

  // Fallback to reference kernel when bias_type is int64 as
  // there is no optimized kernel for int64 bias yet.
  if (bias && bias->type == kTfLiteInt64) {
    reference_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16>(input), GetTensorShape(filter),
        GetTensorData<int8>(filter), GetTensorShape(bias),
        GetTensorData<std::int64_t>(bias), GetTensorShape(output),
        GetTensorData<int16>(output));
  } else if (effective_kernel_type == kReference || has_non_zero_point) {
    reference_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16>(input), GetTensorShape(filter),
        GetTensorData<int8>(filter), GetTensorShape(bias),
        GetTensorData<std::int32_t>(bias), GetTensorShape(output),
        GetTensorData<int16>(output));
  } else {
    optimized_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16_t>(input), GetTensorShape(filter),
        GetTensorData<int8_t>(filter), GetTensorShape(bias),
        GetTensorData<std::int32_t>(bias), GetTensorShape(output),
        GetTensorData<int16_t>(output), GetTensorShape(im2col),
        GetTensorData<int16_t>(im2col),
        CpuBackendContext::GetFromContext(context));
  }
}

template <KernelType kernel_type>
void EvalFloat(TfLiteContext* context, TfLiteNode* node,
               TfLiteConvParams* params, OpData* data,
               const TfLiteTensor* input, const TfLiteTensor* filter,
               const TfLiteTensor* bias, TfLiteTensor* im2col,
               TfLiteTensor* hwcn_weights, TfLiteTensor* output) {

  float output_activation_min, output_activation_max;

//----------------------------------------------------------------
  CalculateActivationRange(activation, &output_activation_min,
                           &output_activation_max);
  KernelType effective_kernel_type = kernel_type;
  // Fall back to the optimized path if multi-threaded conv is unsupported.
  if ((kernel_type == kMultithreadOptimized) &&
      !data->supports_multithreaded_kernel) {
    effective_kernel_type = kGenericOptimized;
  }

  // When im2col is needed (which is implied when 'im2col_oversized' is true),
  // the GEMMM-based optimized path requires im2col data be allocated to ensure
  // the correctness. Therefore, when im2col is disabled because of the
  // oversized temporary im2col tensor, fallback to a non-optimized path is
  // needed.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
    // As detailed by tflite::multithreaded_ops::Conv implementation in
    // multithreaded_conv.h, the Eigen-based execution doesn't need im2col data.
    // Therefore, we could rely on it as a better-optimized fallback than the
    // reference one.
    if (data->supports_multithreaded_kernel) {
      effective_kernel_type = kMultithreadOptimized;
    }
#endif
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = RuntimePaddingType(paddings);
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.stride_width = stride_width;
  op_params.stride_height = stride_height;
  op_params.dilation_width_factor = dilation_width_factor;
  op_params.dilation_height_factor = dilation_height_factor;
  op_params.float_activation_min = output_activation_min;
  op_params.float_activation_max = output_activation_max;
  // std::cout << "codes runs here #5" << std::endl;
  // const int filter_dims_size=4;
  const int32_t* filter_dims_data;
  filter_dims_data = filter_dims_raw;
  const int32_t* bias_dims_data;
  bias_dims_data = bias_dims_raw;

  const float* filter_data;
  filter_data = filter_raw;
  const float* bias_data;
  bias_data = bias_raw;

  switch (effective_kernel_type) {
    case kReference: {
      // std::cout << "codes runs here #6" << std::endl;
      reference_ops::Conv(op_params, GetTensorShape(input),
                          GetTensorData<float>(input), RuntimeShape(filter_dims_size, filter_dims_data),
                          filter_data, RuntimeShape(bias_dims_size, bias_dims_data),
                          bias_data, GetTensorShape(output),
                          GetTensorData<float>(output), GetTensorShape(im2col),
                          GetTensorData<float>(im2col));
      break;
    }
    case kCblasOptimized:
    case kGenericOptimized: {
      // std::cout << "codes runs here #7" << std::endl;
      optimized_ops::Conv(op_params, GetTensorShape(input),
                          GetTensorData<float>(input), RuntimeShape(filter_dims_size, filter_dims_data),
                          filter_data, RuntimeShape(bias_dims_size, bias_dims_data),
                          bias_data, GetTensorShape(output),
                          GetTensorData<float>(output), GetTensorShape(im2col),
                          GetTensorData<float>(im2col),
                          CpuBackendContext::GetFromContext(context));
      break;
    }
    case kMultithreadOptimized: {
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
      
      multithreaded_ops::Conv(
          *eigen_support::GetThreadPoolDevice(context), op_params,
          GetTensorShape(input), GetTensorData<float>(input),
          RuntimeShape(filter_dims_size, filter_dims_data), filter_data, RuntimeShape(bias_dims_size, bias_dims_data),
          bias_data, GetTensorShape(output),
          GetTensorData<float>(output), GetTensorShape(im2col),
          GetTensorData<float>(im2col));
      break;
#else   // !defined(TFLITE_WITH_MULTITHREADED_EIGEN)
      // See Register_CONV_2D: we should never be here when TFLITE_WITH_RUY
      // was enabled. We #if out this code in order to get the corresponding
      // binary size benefits.
      TFLITE_DCHECK(false);
#endif  // defined(TFLITE_WITH_MULTITHREADED_EIGEN)
    }
  }
}

template <KernelType kernel_type>
TfLiteStatus EvalHybridPerChannel(TfLiteContext* context, TfLiteNode* node,
                                  TfLiteConvParams* params, OpData* data,
                                  const TfLiteTensor* input,
                                  const TfLiteTensor* filter,
                                  const TfLiteTensor* bias,
                                  TfLiteTensor* im2col, TfLiteTensor* output) {
  float output_activation_min, output_activation_max;
  CalculateActivationRange(activation, &output_activation_min,
                           &output_activation_max);

  const int batch_size = SizeOfDimension(input, 0);
  TF_LITE_ENSURE(context, batch_size != 0);
  const int input_size = NumElements(input) / batch_size;
  TfLiteTensor* quantized_input_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_quantized_index,
                                     &quantized_input_tensor));
  int8_t* quantized_input_ptr_batch =
      GetTensorData<int8_t>(quantized_input_tensor);
  TfLiteTensor* scaling_factors_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->scaling_factors_index,
                                     &scaling_factors_tensor));
  float* scaling_factors_ptr = GetTensorData<float>(scaling_factors_tensor);
  TfLiteTensor* input_offset_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_offset_index,
                                     &input_offset_tensor));
  int32_t* input_offset_ptr = GetTensorData<int32_t>(input_offset_tensor);

  for (int b = 0; b < batch_size; ++b) {
    const int offset = b * input_size;
    tensor_utils::AsymmetricQuantizeFloats(
        GetTensorData<float>(input) + offset, input_size,
        quantized_input_ptr_batch + offset, &scaling_factors_ptr[b],
        &input_offset_ptr[b]);
  }

  int8_t* im2col_ptr = nullptr;
  int8_t* filter_ptr = nullptr;
  if (im2col != nullptr) {
    im2col_ptr = im2col->data.int8;
  }
  filter_ptr = filter->data.int8;
  const auto* affine_quantization =
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params);

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = PaddingType::kSame;
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.dilation_width_factor = dilation_width_factor;
  op_params.dilation_height_factor = dilation_height_factor;
  op_params.stride_width = stride_width;
  op_params.stride_height = stride_height;
  op_params.float_activation_min = output_activation_min;
  op_params.float_activation_max = output_activation_max;

  switch (effective_kernel_type) {
    case kReference:
      reference_ops::HybridConvPerChannel(
          op_params, scaling_factors_ptr, GetTensorShape(input),
          quantized_input_ptr_batch, GetTensorShape(filter), filter_ptr,
          GetTensorShape(bias), GetTensorData<float>(bias),
          GetTensorShape(output), GetTensorData<float>(output),
          GetTensorShape(im2col), im2col_ptr, affine_quantization->scale->data,
          input_offset_ptr);
      break;
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      TfLiteTensor* row_sums;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->row_sums_index, &row_sums));
      TfLiteTensor* scratch;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->accum_scratch_index, &scratch));
      optimized_ops::HybridConvPerChannel(
          op_params, scaling_factors_ptr, GetTensorShape(input),
          quantized_input_ptr_batch, GetTensorShape(filter), filter_ptr,
          GetTensorShape(bias), GetTensorData<float>(bias),
          GetTensorShape(output), GetTensorData<float>(output),
          GetTensorShape(im2col), im2col_ptr, affine_quantization->scale->data,
          input_offset_ptr, GetTensorShape(scratch),
          GetTensorData<int32>(scratch), GetTensorData<int32_t>(row_sums),
          &data->compute_hybrid_row_sums,
          CpuBackendContext::GetFromContext(context));
      data->compute_hybrid_row_sums = false;
      break;
    }
  }

  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus EvalHybrid(TfLiteContext* context, TfLiteNode* node,
                        TfLiteConvParams* params, OpData* data,
                        const TfLiteTensor* input, const TfLiteTensor* filter,
                        const TfLiteTensor* bias, TfLiteTensor* im2col,
                        TfLiteTensor* accum_scratch, TfLiteTensor* output) {
  float output_activation_min, output_activation_max;
  CalculateActivationRange(activation, &output_activation_min,
                           &output_activation_max);

  const int batch_size = SizeOfDimension(input, 0);
  TF_LITE_ENSURE(context, batch_size != 0);
  const int input_size = NumElements(input) / batch_size;

  const float* input_ptr = GetTensorData<float>(input);
  TfLiteTensor* quantized_input_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_quantized_index,
                                     &quantized_input_tensor));
  int8_t* quantized_input_ptr_batch =
      GetTensorData<int8_t>(quantized_input_tensor);
  TfLiteTensor* scaling_factors_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->scaling_factors_index,
                                     &scaling_factors_tensor));
  float* scaling_factors_ptr = GetTensorData<float>(scaling_factors_tensor);

  // Per-batch input quantization for higher accuracy.
  {
    ruy::profiler::ScopeLabel label("ConvHybridQuantizeInputs");
    for (int b = 0; b < batch_size; ++b) {
      float unused_min, unused_max;
      const int offset = b * input_size;
      tensor_utils::SymmetricQuantizeFloats(
          input_ptr + offset, input_size, quantized_input_ptr_batch + offset,
          &unused_min, &unused_max, &scaling_factors_ptr[b]);
      scaling_factors_ptr[b] *= filter->params.scale;
    }
  }

  switch (kernel_type) {
    case kReference:
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      // There is only one implementation for hybrid kernel.
      ConvParams op_params;
      op_params.padding_type = PaddingType::kSame;
      op_params.padding_values.width = data->padding.width;
      op_params.padding_values.height = data->padding.height;
      op_params.stride_width = stride_width;
      op_params.stride_height = stride_height;
      op_params.dilation_width_factor = dilation_width_factor;
      op_params.dilation_height_factor = dilation_height_factor;
      op_params.float_activation_min = output_activation_min;
      op_params.float_activation_max = output_activation_max;
      if (data->groups == 1) {
        optimized_ops::HybridConv(
            op_params, scaling_factors_ptr, GetTensorShape(input),
            quantized_input_ptr_batch, GetTensorShape(filter),
            GetTensorData<int8_t>(filter), GetTensorShape(bias),
            GetTensorData<float>(bias), GetTensorShape(accum_scratch),
            GetTensorData<int32_t>(accum_scratch), GetTensorShape(output),
            GetTensorData<float>(output), GetTensorShape(im2col),
            GetTensorData<int8_t>(im2col),
            CpuBackendContext::GetFromContext(context));
      } else {
        // This case is handled by (fallbacked to) per channel hybrid group conv
        // and shouldn't hit this branch.
        TF_LITE_KERNEL_LOG(
            context,
            "Group convolution currently not supported for hybrid kernel.");
        return kTfLiteError;
      }
      break;
    }
  }

  return kTfLiteOk;
}

template <KernelType kernel_type, TfLiteType input_type>
TfLiteStatus EvalImpl(TfLiteContext* context, TfLiteNode* node) {
  // std::cout << "codes runs here #2" << std::endl;
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  TfLiteConvParams* params;

  OpData* data = reinterpret_cast<OpData*>(node->user_data);

  TfLiteTensor* output;
  TF_LITE_ENSURE_OK(context, GetOutputSafe(context, node, 0, &output));
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  const TfLiteTensor* filter;
  const int dims_size = filter_dims_size;

  // std::cout << "filter dim size:" << dims_size << std::endl;
  // const int32_t* dims_data = reinterpret_cast<const int32_t*>(dims->data);
  const int32_t* dims_data = filter_dims_raw;
  // bool has_bias = node->inputs->size == 3;
  // const TfLiteTensor* bias = has_bias ? GetInput(context, node, 2) : nullptr;
  const TfLiteTensor* bias = nullptr;
  TfLiteTensor* im2col =
      data->need_im2col
          ? &context->tensors[node->temporaries->data[data->im2col_index]]
          : nullptr;
  TfLiteTensor* hwcn_weights =
      data->need_hwcn_weights
          ? &context->tensors[node->temporaries->data[data->hwcn_weights_index]]
          : nullptr;

  if (data->need_hwcn_weights && !data->have_weights_been_transposed) {
    TransposeFloatTensor(filter, hwcn_weights);
    data->have_weights_been_transposed = true;
  }
  // std::cout << "codes runs here #3" << std::endl;

  TFLITE_DCHECK_EQ(input_type, input->type);
  switch (input_type) {  // Already know in/outtypes are same.
    case kTfLiteFloat32:
      if (filter_type == kTfLiteUInt8 || filter_type == kTfLiteInt8) {
        if (data->is_hybrid_per_channel ||
            // TODO(b/162870360): Fallback to PerChannel implementation
            // before we have grouped hybrid convolution.
            data->groups != 1) {
          TF_LITE_ENSURE_OK(context, EvalHybridPerChannel<kernel_type>(
                                         context, node, params, data, input,
                                         filter, bias, im2col, output));
        } else {
          TfLiteTensor* accum_scratch =
              &context->tensors[node->temporaries
                                    ->data[data->accum_scratch_index]];
          TF_LITE_ENSURE_OK(context,
                            EvalHybrid<kernel_type>(context, node, params, data,
                                                    input, filter, bias, im2col,
                                                    accum_scratch, output));
        }
      } else {
        EvalFloat<kernel_type>(context, node, params, data, input, filter, bias,
                               im2col, hwcn_weights, output);
      }
      break;
    case kTfLiteUInt8:
      EvalQuantized<kernel_type>(context, node, params, data, input, filter,
                                 bias, im2col, output);
      break;
    case kTfLiteInt8:
      EvalQuantizedPerChannel<kernel_type>(context, node, params, data, input,
                                           filter, bias, output, im2col);
      break;
    case kTfLiteInt16:
      EvalQuantizedPerChannel16x8<kernel_type>(
          context, node, params, data, input, filter, bias, output, im2col);
      break;
    default:
      TF_LITE_KERNEL_LOG(context, "Type %s currently not supported.",
                         TfLiteTypeGetName(input->type));
      return kTfLiteError;
  }
  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus Eval(TfLiteContext* context, TfLiteNode* node) {
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));

  switch (input->type) {
    case kTfLiteFloat32:
      return EvalImpl<kernel_type, kTfLiteFloat32>(context, node);
    case kTfLiteUInt8:
      return EvalImpl<kernel_type, kTfLiteUInt8>(context, node);
    case kTfLiteInt8:
      return EvalImpl<kernel_type, kTfLiteInt8>(context, node);
    case kTfLiteInt16:
      return EvalImpl<kernel_type, kTfLiteInt16>(context, node);
    default:
      TF_LITE_KERNEL_LOG(context, "Type %s not currently supported.",
                         TfLiteTypeGetName(input->type));
      return kTfLiteError;
  }
}

}  // namespace conv

TfLiteRegistration* Register_krubxg_REF() {
  static TfLiteRegistration r = {krubxg::Init, krubxg::Free,
                                 krubxg::Prepare<krubxg::kReference>,
                                 krubxg::Eval<krubxg::kReference>};
  return &r;
}

TfLiteRegistration* Register_krubxg_GENERIC_OPT() {
  static TfLiteRegistration r = {krubxg::Init, krubxg::Free,
                                 krubxg::Prepare<krubxg::kGenericOptimized>,
                                 krubxg::Eval<krubxg::kGenericOptimized>};
  return &r;
}

TfLiteRegistration* Register_krubxg_MULTITHREADED_OPT() {
  static TfLiteRegistration r = {krubxg::Init, krubxg::Free,
                                 krubxg::Prepare<krubxg::kMultithreadOptimized>,
                                 krubxg::Eval<krubxg::kMultithreadOptimized>};
  return &r;
}

// TfLiteRegistration* Register_krubxg_CBLAS_OPT() {
//   static TfLiteRegistration r = {krubxg::Init, krubxg::Free,
//                                  krubxg::Prepare<krubxg::kCblasOptimized>,
//                                  krubxg::Eval<krubxg::kCblasOptimized>};
//   return &r;
// }

TfLiteRegistration* Register_krubxg() {
#if defined TFLITE_WITH_MULTITHREADED_EIGEN
  return Register_krubxg_MULTITHREADED_OPT();
#else
  return Register_krubxg_GENERIC_OPT();
#endif
}


}  // namespace builtin
}  // namespace ops
}  // namespace tflite
